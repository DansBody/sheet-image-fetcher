services:
  - type: web
    name: sheet-image-fetcher
    runtime: node
    plan: free
    buildCommand: npm install && npx playwright install --with-deps --only-shell chromium
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 22
